# SRMS-Project
A C-based student management system with login roles (Admin, Staff, User, Guest). Supports adding, viewing, searching, updating, and deleting student records stored in text files. Includes credential handling, role-based access, and menu-driven interaction.
